# Disabled — this workflow checks Heroku review apps which are Chatwoot-specific.
# Re-enable with Whisker-specific deployment check when needed.

name: Deploy Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment_check:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled."
