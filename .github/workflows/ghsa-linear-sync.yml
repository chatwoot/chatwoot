# Disabled — requires Linear integration secrets that are not configured for Whisker.
# Re-enable when Linear integration is set up.

name: Sync GHSA advisories to Linear

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled."
