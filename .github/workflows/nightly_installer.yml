# Disabled — this workflow installs Chatwoot, not Whisker.
# Re-enable with a Whisker-specific installer when available.

name: Run Linux nightly installer
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nightly:
    runs-on: ubuntu-24.04
    if: false
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled."
