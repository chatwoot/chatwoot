import merge from 'lodash/merge'; export default { EDIT_CONTACT(state, contact) { const index = state.records.findIndex(c => c.id === contact.id); if (index !== -1) { const existingContact = state.records[index]; const updatedContact = merge({}, existingContact, contact); state.records.splice(index, 1, updatedContact); } } };