import Kanban from './Kanban.vue';
import { frontendURL } from '../../../helper/URLHelper';

export const routes = [
  {
    path: frontendURL('accounts/:accountId/kanban'),
    name: 'kanban',
    component: Kanban,
    meta: {
      icon: 'lucide:columns',      // kanban-style icon
      label: 'Kanban',             // sidebar text
      showInMenu: true,            // make it appear in the left menu
      permissions: ['administrator'], // who can see it (optional)
    },
  },
];
