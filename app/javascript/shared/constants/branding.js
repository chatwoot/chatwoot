export const DEFAULT_BRAND_NAMES = ['Chatwoot', 'doozadesk', 'Dooza Desk'];

export const DEFAULT_INSTALLATION_NAMES = ['doozadesk', 'Dooza Desk'];

export const isDefaultInstallationName = installationName =>
  DEFAULT_INSTALLATION_NAMES.includes(installationName);
