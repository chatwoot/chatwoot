class Widget::TokenService < BaseTokenService
  # Widget::TokenService now inherits all common functionality from BaseTokenService
  # The payload and token parameters work the same way as before
end
