<p>Hello {{ account.name }} Team,</p>

<p><strong>Cruise Control Impact Report - {{ start_date | date: "%m/%d" }} - {{ end_date | date: "%m/%d" }}</strong></p>

<p>Here’s a snapshot of your dealership’s performance for the week:</p>

<table width="100%" cellpadding="0" cellspacing="0" border="0" style="margin-bottom: 20px;">
  <tr><td><strong>Dealership name:</strong> {{ dealership_name }}</td></tr>
  <tr><td><strong>New conversations:</strong> {{ new_conversations }}</td></tr>
  <tr><td><strong>Booking requests sent:</strong> {{ booking_links_sent }}</td></tr>
  <tr><td><strong>Completed booking requests:</strong> {{ booking_forms_completed }}</td></tr>
  <tr><td><strong>Bookings conversion rate:</strong> {{ conversion_rate }}%</td></tr>
  <tr><td><strong>Estimated value:</strong> ${{ estimated_value }}</td></tr>
</table>

<p>Visit our reports dashboard for a more detailed breakdown:</p>

<table cellpadding="0" cellspacing="0" border="0" style="margin-top: 10px;">
  <tr>
    <td>
      <a href="{{ report_url }}" style="display: inline-block; padding: 10px 16px; background-color: #1f93ff; color: #ffffff; text-decoration: none; border-radius: 4px; font-weight: bold;">
        Reports Dashboard
      </a>
    </td>
  </tr>
</table>

<p>Best regards,<br>The Cruise Control Team</p>
