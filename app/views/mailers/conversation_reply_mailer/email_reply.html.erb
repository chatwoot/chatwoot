<tr>
  <td>
    <p style="font-family: Roboto,"Helvetica Neue",Tahoma,Arial,sans-serif; text-align: start; unicode-bidi: plaintext;">
      Hello, <br/>
      Here's the summary of conversation with <strong><%= @account.name %></strong> through <strong><%= @inbox.channel.name %></strong>,
      <br />
    </p>
  </td>
</tr>
<% if @message.content %>
  <%= CommonMarker.render_html(@message.content).html_safe %>
<% end %>
<% if @message.attachments %>
  <% @message.attachments.each do |attachment| %>
    attachment [<a href="<%= attachment.file_url %>" _target="blank">click here to view</a>]
  <% end %>
<% end %>
