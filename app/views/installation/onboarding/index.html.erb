<!DOCTYPE html>
<html>
  <head>
    <title>SuperAdmin | ExiZap</title>
    <%= javascript_pack_tag 'superadmin' %>
    <%= stylesheet_pack_tag 'superadmin' %>
  </head>
  <body data-gr-c-s-loaded="true">
    <div id="app" class="superadmin-body app-wrapper app-root">
      <div class="medium column login">
        <div class="text-center medium-12 login__hero align-self-top">
          <img
            src="/brand-assets/logo.svg"
            alt="Chatwoot logo"
            class="hero__logo"
          />
          <h2 class="hero--title">
            Olá, bem-vindo ao ExiZap 👋
          </h2>
        </div>
        <div class="row align-center">
          <div class="small-12 medium-4 column">
            <%= form_tag('/installation/onboarding', class: 'login-box column align-self-top') do %>
              <div class="column log-in-form">
                <% if flash[:error].present? %>
                <div data-alert class="alert-box warning"><%= flash[:error] %></div>
                <% end %>
                <label>
                  <span>Nome</span>
                  <%= text_field :user, :name, placeholder: "Escreva seu nome completo. ex.: Bruce Wayne", required: true %>
                </label>
                <label>
                  <span>Nome Empresa</span>
                  <%= text_field :user, :company, placeholder: "Digite o nome da sua empresa. ex.: Wayne Enterprises", required: true %>
                </label>
                <label>
                  <span>Seu Email</span>
                  <%= email_field :user, :email, placeholder: "Digite seu endereço de e-mail de trabalho. por exemplo: bruce@wayne.enterprises", required: true %>
                </label>
                <label>
                  <span>Sua Senha</span>
                  <%= password_field :user, :password, placeholder: "Digite uma senha com 6 caracteres ou mais.", required: true %>
                </label>
                <div class="update-subscription--checkbox">
                  <%= check_box_tag "subscribe_to_updates", 'true', true %>
                  <label for="subscribe_to_updates">
                    Assinar notas de lançamentos de versões, boletins informativos e pesquisas de feedback de produtos.
                  </label>
                </div>
                <button type="submit" class="button nice large expanded">
                  Fim Setup
                </button>
              </div>
            <% end %>
          </div>
        </div>
    </div>
  </body>
</html>
