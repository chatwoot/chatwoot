<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <title><%= t('public_preferences.page_title', account: @account&.name || 'CommMate') %></title>
    <%= csrf_meta_tags %>
    <%= vite_client_tag %>
    <%= vite_stylesheet_tag 'portal' %>

    <script>
      // Dark mode detection
      if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      }
    </script>

    <style>
      /* Chatwoot Next Design System Colors */
      :root {
        --slate-1: 252 252 253;
        --slate-2: 249 249 251;
        --slate-3: 240 240 243;
        --slate-4: 232 232 236;
        --slate-5: 224 225 230;
        --slate-6: 217 217 224;
        --slate-7: 205 206 214;
        --slate-8: 185 187 198;
        --slate-9: 139 141 152;
        --slate-10: 128 131 141;
        --slate-11: 96 100 108;
        --slate-12: 28 32 36;
        --teal-9: 18 165 148;
        --teal-10: 13 155 138;
        --ruby-9: 229 70 102;
        --ruby-10: 220 59 93;
        --background-color: 253 253 253;
        --solid-1: 255 255 255;
        --border-weak: 234 234 234;
        --brand: #107e44;
      }

      .dark {
        --slate-1: 17 17 19;
        --slate-2: 24 25 27;
        --slate-3: 33 34 37;
        --slate-4: 39 42 45;
        --slate-5: 46 49 53;
        --slate-6: 54 58 63;
        --slate-7: 67 72 78;
        --slate-8: 90 97 105;
        --slate-9: 105 110 119;
        --slate-10: 119 123 132;
        --slate-11: 176 180 186;
        --slate-12: 237 238 240;
        --teal-9: 18 165 148;
        --teal-10: 14 179 158;
        --ruby-9: 229 70 102;
        --ruby-10: 236 90 114;
        --background-color: 18 18 19;
        --solid-1: 23 23 26;
        --border-weak: 38 38 42;
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: -apple-system, system-ui, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        background-color: rgb(var(--background-color));
        color: rgb(var(--slate-12));
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 1rem;
      }

      /* Main Card - Following Chatwoot Auth Pattern */
      .preferences-card {
        background: rgb(var(--solid-1));
        border-radius: 0.75rem;
        box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
        width: 100%;
        max-width: 420px;
        overflow: hidden;
      }

      .card-header {
        padding: 1.5rem 1.5rem 0;
        text-align: center;
      }

      .card-logo {
        height: 2rem;
        width: auto;
        margin-bottom: 1rem;
      }

      .card-title {
        font-size: 1.5rem;
        font-weight: 500;
        color: rgb(var(--slate-12));
        margin-bottom: 0.25rem;
      }

      .card-subtitle {
        font-size: 0.875rem;
        color: rgb(var(--slate-11));
      }

      .card-body {
        padding: 1.5rem;
      }

      .card-description {
        font-size: 0.875rem;
        color: rgb(var(--slate-11));
        margin-bottom: 1.25rem;
        line-height: 1.5;
      }

      /* Form Elements */
      .form-group {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin-bottom: 1.25rem;
      }

      .checkbox-item {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        padding: 0.875rem;
        background: rgb(var(--slate-2));
        border: 1px solid rgb(var(--border-weak));
        border-radius: 0.5rem;
        cursor: pointer;
        transition: all 0.15s ease;
      }

      .checkbox-item:hover {
        border-color: var(--brand);
        background: rgb(var(--slate-3));
      }

      .checkbox-item input[type="checkbox"] {
        width: 1.125rem;
        height: 1.125rem;
        accent-color: var(--brand);
        cursor: pointer;
        flex-shrink: 0;
        margin-top: 0.125rem;
      }

      .checkbox-content {
        flex: 1;
        min-width: 0;
      }

      .checkbox-label {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 0.5rem;
      }

      .checkbox-text {
        font-size: 0.875rem;
        font-weight: 500;
        color: rgb(var(--slate-12));
        line-height: 1.4;
      }

      .checkbox-text.muted {
        color: rgb(var(--slate-10));
        font-style: italic;
        font-weight: 400;
      }

      .label-color {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 50%;
        flex-shrink: 0;
        margin-top: 0.375rem;
      }

      /* Tooltip */
      .info-tooltip {
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: rgb(var(--slate-9));
        cursor: help;
        flex-shrink: 0;
        font-size: 0.75rem;
      }

      .info-tooltip:hover .tooltip-text {
        opacity: 1;
        visibility: visible;
      }

      .tooltip-text {
        position: absolute;
        bottom: calc(100% + 0.5rem);
        left: 50%;
        transform: translateX(-50%);
        background: rgb(var(--slate-12));
        color: rgb(var(--slate-1));
        padding: 0.375rem 0.625rem;
        border-radius: 0.375rem;
        font-size: 0.75rem;
        font-weight: 400;
        font-style: normal;
        white-space: nowrap;
        opacity: 0;
        visibility: hidden;
        transition: all 0.15s ease;
        z-index: 10;
      }

      .tooltip-text::after {
        content: '';
        position: absolute;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        border: 5px solid transparent;
        border-top-color: rgb(var(--slate-12));
      }

      /* Buttons */
      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.75rem 1rem;
        border-radius: 0.5rem;
        font-size: 0.875rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.15s ease;
        border: none;
        width: 100%;
        text-decoration: none;
      }

      .btn-primary {
        background-color: var(--brand);
        color: white;
      }

      .btn-primary:hover {
        filter: brightness(1.1);
      }

      .btn-secondary {
        background-color: rgb(var(--slate-3));
        color: rgb(var(--slate-12));
        border: 1px solid rgb(var(--border-weak));
      }

      .btn-secondary:hover {
        background-color: rgb(var(--slate-4));
      }

      .btn-danger {
        background-color: transparent;
        color: rgb(var(--ruby-9));
        border: 1px solid rgb(var(--ruby-9));
      }

      .btn-danger:hover {
        background-color: rgb(var(--ruby-9));
        color: white;
      }

      .btn svg {
        width: 1rem;
        height: 1rem;
      }

      /* Divider */
      .divider {
        height: 1px;
        background: rgb(var(--border-weak));
        margin: 1.25rem 0;
      }

      /* Success Alert */
      .alert {
        display: flex;
        align-items: center;
        gap: 0.625rem;
        padding: 0.75rem 1rem;
        border-radius: 0.5rem;
        margin-bottom: 1rem;
        font-size: 0.875rem;
        font-weight: 500;
      }

      .alert-success {
        background-color: rgba(18, 165, 148, 0.1);
        color: rgb(var(--teal-10));
      }

      .alert svg {
        width: 1.125rem;
        height: 1.125rem;
        flex-shrink: 0;
      }

      /* Status Page */
      .status-page {
        text-align: center;
        padding: 2rem 1.5rem;
      }

      .status-icon {
        width: 4rem;
        height: 4rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.25rem;
      }

      .status-icon svg {
        width: 2rem;
        height: 2rem;
      }

      .status-icon.success {
        background-color: rgba(18, 165, 148, 0.15);
        color: rgb(var(--teal-9));
      }

      .status-icon.warning {
        background-color: rgba(255, 197, 61, 0.15);
        color: #d97706;
      }

      .status-icon.error {
        background-color: rgba(229, 70, 102, 0.15);
        color: rgb(var(--ruby-9));
      }

      .status-icon.neutral {
        background-color: rgb(var(--slate-3));
        color: rgb(var(--slate-9));
      }

      .status-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: rgb(var(--slate-12));
        margin-bottom: 0.5rem;
      }

      .status-message {
        font-size: 0.875rem;
        color: rgb(var(--slate-11));
        line-height: 1.5;
        margin-bottom: 0.75rem;
      }

      .status-note {
        font-size: 0.75rem;
        color: rgb(var(--slate-10));
        margin-top: 0.5rem;
      }

      /* Label Badge */
      .label-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        background: rgb(var(--slate-3));
        border-radius: 2rem;
        margin-top: 0.75rem;
      }

      .label-badge .color-dot {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 50%;
      }

      .label-badge .label-name {
        font-size: 0.875rem;
        font-weight: 500;
        color: rgb(var(--slate-12));
      }

      .label-badge.strikethrough .label-name {
        text-decoration: line-through;
        opacity: 0.7;
      }

      /* Empty State */
      .empty-state {
        text-align: center;
        padding: 2rem 1rem;
        color: rgb(var(--slate-10));
      }

      .empty-state svg {
        width: 3rem;
        height: 3rem;
        margin-bottom: 0.75rem;
        opacity: 0.5;
      }

      .empty-state p {
        font-size: 0.875rem;
      }

      /* Unsubscribe Section */
      .unsubscribe-section {
        text-align: center;
        padding-top: 0.25rem;
      }

      .unsubscribe-hint {
        font-size: 0.75rem;
        color: rgb(var(--slate-10));
        margin-bottom: 0.75rem;
      }

      /* Actions */
      .action-links {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin-top: 1.25rem;
      }

      .action-links .card-subtitle {
        margin-bottom: 0.25rem;
      }

      .link {
        color: var(--brand);
        text-decoration: none;
        font-size: 0.875rem;
        font-weight: 500;
        text-align: center;
        padding: 0.625rem 1rem;
        border-radius: 0.375rem;
        transition: background-color 0.15s ease;
        display: block;
      }

      .link:hover {
        text-decoration: underline;
        background-color: rgba(16, 126, 68, 0.08);
      }

      .link:active {
        background-color: rgba(16, 126, 68, 0.15);
      }

      /* Footer */
      .page-footer {
        margin-top: 1.5rem;
        text-align: center;
      }

      .page-footer a {
        font-size: 0.75rem;
        color: rgb(var(--slate-10));
        text-decoration: none;
        transition: color 0.15s ease;
      }

      .page-footer a:hover {
        color: var(--brand);
      }

      /* Contact Info */
      .contact-info {
        font-size: 0.75rem;
        color: rgb(var(--slate-10));
        text-align: center;
        margin-top: 1rem;
      }
    </style>
  </head>
  <body>
    <div id="preferences-content">
      <%= yield %>
    </div>

    <footer class="page-footer">
      <a href="https://www.commmate.com" target="_blank" rel="noopener noreferrer">
        <%= t('public_preferences.footer.powered_by') %>
      </a>
    </footer>

  </body>
</html>
