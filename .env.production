# Production Environment Variables for VegaVision Helpdesk
# This file should be uploaded to GitLab Secure Files as ".env"
# GitLab: Settings → CI/CD → Secure Files → Upload File

# Application Settings
NODE_ENV=production
RAILS_ENV=production
INSTALLATION_ENV=docker
RAILS_SERVE_STATIC_FILES=true

# Database Configuration 
# Update with your external PostgreSQL database details
DATABASE_URL=postgresql://username:password@your-db-host:5432/vegavision_production
POSTGRES_HOST=your-db-host
POSTGRES_PORT=5432
POSTGRES_DATABASE=vegavision_production
POSTGRES_USERNAME=your-db-username
POSTGRES_PASSWORD=your-db-password

# Redis Configuration
# Uses internal Redis container - only change password
REDIS_URL=redis://:your-redis-password@127.0.0.1:6377/0
REDIS_PASSWORD=your-redis-password

# Application Secrets (Generate secure values)
# Generate SECRET_KEY_BASE with: docker run --rm charlesmbv/vegavision:latest rails secret
SECRET_KEY_BASE=your-secret-key-base-here
FRONTEND_URL=https://your-domain.com

# Email Configuration (Update with your SMTP settings)
MAILER_SENDER_EMAIL=noreply@your-domain.com
SMTP_DOMAIN=your-domain.com
SMTP_ADDRESS=smtp.your-provider.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_AUTHENTICATION=plain
SMTP_ENABLE_STARTTLS_AUTO=true

# Storage Configuration
ACTIVE_STORAGE_SERVICE=local
STORAGE_BUCKET_NAME=vegavision-storage

# Security & Logging
FORCE_SSL=false
RAILS_LOG_TO_STDOUT=true

# Optional: Third-party integrations
# SENTRY_DSN=your-sentry-dsn
# GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id
# GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret

# Custom: Add any additional environment variables your setup needs
# CUSTOM_FEATURE_FLAG=enabled
# EXTERNAL_API_KEY=your-api-key
