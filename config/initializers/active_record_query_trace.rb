# Disabled for dev performance — captures a backtrace on every SQL query.
# Re-enable when debugging N+1 queries.
# ActiveRecordQueryTrace.enabled = true if Rails.env.development?
ActiveRecordQueryTrace.enabled = false
