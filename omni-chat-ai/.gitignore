.env
*.pyc
__pycache__/
.venv/
.ruff_cache/
.pytest_cache/

# Track the project Claude Code workspace (agents, commands, settings, hooks) —
# overrides the repo-root `.claude/` ignore. Keep personal/local files out.
!.claude/
!.claude/**
.claude/settings.local.json

# editable-install build metadata
*.egg-info/
