apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatwoot-vite-staging
  annotations:
    kubernetes.io/change-cause: "Staging deployment"
    container.apparmor.security.beta.kubernetes.io/vite: runtime/default
spec:
  replicas: 1
  progressDeadlineSeconds: 600
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: chatwoot-vite-staging
  template:
    metadata:
      labels:
        app: chatwoot-vite-staging
      annotations:
        timestamp: "0"
    spec:
      containers:
      - name: vite
        image: wavai/chatwoot-vite-staging:v1.0.68
        imagePullPolicy: Always
        command: ["/bin/sh"]
        args: ["-c", "docker/entrypoints/vite.sh"]
        ports:
        - containerPort: 3036
        envFrom:
        - configMapRef:
            name: chatwoot-config-staging
        - secretRef:
            name: chatwoot-secrets-staging
        env:
        - name: VITE_DEV_SERVER_HOST
          value: "0.0.0.0"
        - name: NODE_ENV
          value: "production"
        - name: RAILS_ENV
          value: "production"
        - name: VITE_PORT
          value: "3036"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: chatwoot-vite-staging
spec:
  selector:
    app: chatwoot-vite-staging
  ports:
  - port: 3036
    targetPort: 3036
  type: ClusterIP
