version: '3.9'

services:
  VV-MBVIT-Helpdesk-Base: &base
    image: charlesmbv/vegavision:latest
    env_file: .env
    volumes:
      - /vv-mbvit-prod-volumes/storage_data:/app/storage

  VV-MBVIT-Helpdesk-Rails:
    <<: *base
    container_name: VV-MBVIT-Helpdesk-Rails
    depends_on:
      - VV-MBVIT-Helpdesk-Postgres
      - VV-MBVIT-Helpdesk-Redis
    ports:
      - '127.0.0.1:3001:3000'
    environment:
      - NODE_ENV=production
      - RAILS_ENV=production
      - INSTALLATION_ENV=docker
      - RAILS_SERVE_STATIC_FILES=true
    entrypoint: docker/entrypoints/rails.sh
    command: ['bundle', 'exec', 'rails', 's', '-p', '3000', '-b', '0.0.0.0']
    restart: always
    networks:
      - VV-MBVIT-Helpdesk-Internal
      - public-proxy

  VV-MBVIT-Helpdesk-Sidekiq:
    <<: *base
    container_name: VV-MBVIT-Helpdesk-Sidekiq
    depends_on:
      - VV-MBVIT-Helpdesk-Postgres
      - VV-MBVIT-Helpdesk-Redis
    environment:
      - NODE_ENV=production
      - RAILS_ENV=production
      - INSTALLATION_ENV=docker
    command: ['bundle', 'exec', 'sidekiq', '-C', 'config/sidekiq.yml']
    restart: always
    networks:
      - VV-MBVIT-Helpdesk-Internal
      - public-proxy

  VV-MBVIT-Helpdesk-Postgres:
    image: pgvector/pgvector:pg16
    container_name: VV-MBVIT-Helpdesk-Postgres
    restart: always
    ports:
      - '127.0.0.1:5434:5432'
    volumes:
      - /vv-mbvit-prod-volumes/postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=chatwoot
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Password123$
    networks:
      - VV-MBVIT-Helpdesk-Internal

  VV-MBVIT-Helpdesk-Redis:
    image: redis:alpine
    container_name: VV-MBVIT-Helpdesk-Redis
    restart: always
    command: ["sh", "-c", "redis-server --requirepass \"$REDIS_PASSWORD\""]
    env_file: .env
    ports:
      - '127.0.0.1:6377:6379'
    volumes:
      - /vv-mbvit-prod-volumes/redis_data:/data
    networks:
      - VV-MBVIT-Helpdesk-Internal

volumes:
  # Volume keys are still needed, even when paths are absolute
  storage_data:
  postgres_data:
  redis_data:

networks:
  VV-MBVIT-Helpdesk-Internal:
    name: VV-MBVIT-Helpdesk-Internal
    driver: bridge

  public-proxy:
    external: true
