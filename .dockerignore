.bundle
.env
.env.*
docker-compose.*
docker/Dockerfile
docker/dockerfiles
log
storage
public/system
tmp
.codeclimate.yml
public/packs
node_modules
vendor/bundle
.DS_Store
*.swp
*~
# CUSTOMIZAÇÃO_SYNAPSEOS — exclusões pesadas que travavam o build context
# transfer (Mac local). GIT_SHA é passado via --build-arg, então .git
# não é necessário dentro do contexto.
.git
.pnpm-store
.claude
.venv
.pytest_cache
spec
test
coverage
