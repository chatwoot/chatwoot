"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var __1 = require("..");
describe('connection', function () {
    var online = true;
    Object.defineProperty(window.navigator, 'onLine', {
        get: function () {
            return online;
        },
    });
    test('checks that the browser is online', function () {
        online = true;
        expect(__1.isOnline()).toBe(true);
        expect(__1.isOffline()).toBe(false);
    });
    test('checks that the browser is offline', function () {
        online = false;
        expect(__1.isOnline()).toBe(false);
        expect(__1.isOffline()).toBe(true);
    });
});
//# sourceMappingURL=index.test.js.map