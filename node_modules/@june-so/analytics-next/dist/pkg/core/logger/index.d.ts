export declare type LogLevel = 'debug' | 'info' | 'warn' | 'error';
export declare type LogMessage = {
    level: LogLevel;
    message: string;
    time?: Date;
    extras?: object & {
        [key: string]: any;
    };
};
export default class Logger {
    private _logs;
    log: (level: LogLevel, message: string, extras?: object | undefined) => void;
    get logs(): LogMessage[];
    flush(): void;
}
