
{{alias}}()
    Tests for native `Symbol` support.

    Returns
    -------
    bool: boolean
        Boolean indicating if an environment has native `Symbol` support.

    Examples
    --------
    > var bool = {{alias}}()
    <boolean>

    See Also
    --------

