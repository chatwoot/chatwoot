
{{alias}}()
    Tests for native `Float32Array` support.

    Returns
    -------
    bool: boolean
        Boolean indicating if an environment has `Float32Array` support.

    Examples
    --------
    > var bool = {{alias}}()
    <boolean>

    See Also
    --------

