declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts value has an own or nested named property which is an Array of numbers.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveArrayOfNumbers('scores')
             * );
             */
            toHaveArrayOfNumbers<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts value has an own or nested named property which is an Array of numbers.
             * @example
             * expect({ foo: { bar: [1, new Number(8)] } }).toHaveArrayOfNumbers('foo.bar');
             */
            toHaveArrayOfNumbers(propPath: string): R;
        }
    }
}
export declare const toHaveArrayOfNumbersMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
