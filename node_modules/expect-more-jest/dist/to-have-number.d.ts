declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a number or `new Number`.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveNumber('foo.bar')
             * );
             */
            toHaveNumber<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a number or `new Number`.
             * @example
             * expect({ foo: { bar: X } }).toHaveNumber('foo.bar');
             */
            toHaveNumber(propPath: string): R;
        }
    }
}
export declare const toHaveNumberMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
