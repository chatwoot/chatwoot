declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is false or `new False()`.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveFalse('foo.bar')
             * );
             */
            toHaveFalse<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is false or `new False()`.
             * @example
             * expect({ foo: { bar: false } }).toHaveFalse('foo.bar');
             */
            toHaveFalse(propPath: string): R;
        }
    }
}
export declare const toHaveFalseMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
