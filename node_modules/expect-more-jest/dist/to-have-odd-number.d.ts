declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is an odd number.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveOddNumber('foo.bar')
             * );
             */
            toHaveOddNumber<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is an odd number.
             * @example
             * expect({ foo: { bar: X } }).toHaveOddNumber('foo.bar');
             */
            toHaveOddNumber(propPath: string): R;
        }
    }
}
export declare const toHaveOddNumberMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
