declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a string same or array length as the given
             * string or array.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveSameLengthAs('foo.bar')
             * );
             */
            toHaveSameLengthAs<T>(propPath: string, other: string | any[]): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a string same or array length as the given
             * string or array.
             * @example
             * expect({ foo: { bar: X } }).toHaveSameLengthAs('foo.bar');
             */
            toHaveSameLengthAs(propPath: string, other: string | any[]): R;
        }
    }
}
export declare const toHaveSameLengthAsMatcher: (received: any, propPath: string, other: string | any[]) => {
    message: () => string;
    pass: boolean;
};
