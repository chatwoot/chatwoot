declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts value has an own or nested named property which is an Array of objects.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveArrayOfObjects('friends')
             * );
             */
            toHaveArrayOfObjects<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts value has an own or nested named property which is an Array of objects.
             * @example
             * expect({ foo: { bar: [{}, new Object()] } }).toHaveArrayOfObjects('foo.bar');
             */
            toHaveArrayOfObjects(propPath: string): R;
        }
    }
}
export declare const toHaveArrayOfObjectsMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
