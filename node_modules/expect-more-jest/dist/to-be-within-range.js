"use strict";
exports.__esModule = true;
var expect_more_1 = require("expect-more");
var create_result_1 = require("./lib/create-result");
exports.toBeWithinRangeMatcher = function (received, floor, ceiling) {
    return create_result_1.createResult({
        message: function () { return "expected " + received + " to be within range " + floor + " - " + ceiling + " (inclusive}"; },
        notMessage: function () { return "expected " + received + " not to be within range " + floor + " - " + ceiling + " (inclusive}"; },
        pass: expect_more_1.isWithinRange(floor, ceiling, received)
    });
};
expect.extend({ toBeWithinRange: exports.toBeWithinRangeMatcher });
