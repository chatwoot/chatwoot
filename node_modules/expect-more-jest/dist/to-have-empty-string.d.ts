declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is an empty string.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveEmptyString('foo.bar')
             * );
             */
            toHaveEmptyString<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is an empty string.
             * @example
             * expect({ foo: { bar: '' } }).toHaveEmptyString('foo.bar');
             */
            toHaveEmptyString(propPath: string): R;
        }
    }
}
export declare const toHaveEmptyStringMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
