declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a method (function).
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveMethod('foo.bar')
             * );
             */
            toHaveMethod<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a method (function).
             * @example
             * expect({ foo: { bar: X } }).toHaveMethod('foo.bar');
             */
            toHaveMethod(propPath: string): R;
        }
    }
}
export declare const toHaveMethodMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
