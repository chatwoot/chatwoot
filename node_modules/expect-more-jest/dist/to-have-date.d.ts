declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a date.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveDate('foo.bar')
             * );
             */
            toHaveDate<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a date.
             * @example
             * expect({ foo: { bar: new Date() } }).toHaveDate('foo.bar');
             */
            toHaveDate(propPath: string): R;
        }
    }
}
export declare const toHaveDateMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
