declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is an instance of `Date`.
             * @example
             * expect(onSubmit).toHaveBeenCalledWith(expect.objectContaining({ releaseDate: expect.toBeDate() }));
             */
            toBeDate<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is an instance of `Date`.
             * @example
             * expect(game.releaseDate).toBeDate();
             */
            toBeDate(): R;
        }
    }
}
export declare const toBeDateMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
