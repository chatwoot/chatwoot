declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is calculable.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveCalculable('foo.bar')
             * );
             */
            toHaveCalculable<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is calculable.
             * @example
             * expect({ foo: { bar: X } }).toHaveCalculable('foo.bar');
             */
            toHaveCalculable(propPath: string): R;
        }
    }
}
export declare const toHaveCalculableMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
