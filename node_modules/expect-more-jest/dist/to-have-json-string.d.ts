declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a JSON string.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveJsonString('foo.bar')
             * );
             */
            toHaveJsonString<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a JSON string.
             * @example
             * expect({ foo: { bar: '{}' } }).toHaveJsonString('foo.bar');
             */
            toHaveJsonString(propPath: string): R;
        }
    }
}
export declare const toHaveJsonStringMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
