declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is a `true` or `new Boolean(true)`.
             * @param other
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ isActive: expect.toBeTrue() }));
             */
            toBeTrue<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is a `true` or `new Boolean(true)`.
             * @example
             * expect(player.isActive).toBeTrue();
             */
            toBeTrue(): R;
        }
    }
}
export declare const toBeTrueMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
