declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a date after the given date.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveDateAfter('foo.bar')
             * );
             */
            toHaveDateAfter<T>(propPath: string, otherDate: Date): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a date after the given date.
             * @example
             * expect({ foo: { bar: X } }).toHaveDateAfter('foo.bar');
             */
            toHaveDateAfter(propPath: string, otherDate: Date): R;
        }
    }
}
export declare const toHaveDateAfterMatcher: (received: any, propPath: string, otherDate: Date) => {
    message: () => string;
    pass: boolean;
};
