declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is an `Array` containing at least 1 value.
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ items: expect.toBeNonEmptyArray() }));
             */
            toBeNonEmptyArray<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is an `Array` containing at least 1 value.
             * @example
             * expect(basket.items).toBeNonEmptyArray();
             */
            toBeNonEmptyArray(): R;
        }
    }
}
export declare const toBeNonEmptyArrayMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
