declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is a `Number` divisible by `other` number.
             * @param divisor
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ paws: expect.toBeDivisibleBy(2) }));
             */
            toBeDivisibleBy<T>(ber: any, divisor: any): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is a `Number` divisible by `other` number.
             * @param divisor
             * @example
             * expect(cat.paws).toBeDivisibleBy(2);
             */
            toBeDivisibleBy(ber: any, divisor: any): R;
        }
    }
}
export declare const toBeDivisibleByMatcher: (received: number, divisor: any) => {
    message: () => string;
    pass: boolean;
};
