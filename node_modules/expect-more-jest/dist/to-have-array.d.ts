declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts value has an own or nested named property which is an Array.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveArray('messages')
             * );
             */
            toHaveArray<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts value has an own or nested named property which is an Array.
             * @example
             * expect({ foo: { bar: [] } }).toHaveArray('foo.bar');
             */
            toHaveArray(propPath: string): R;
        }
    }
}
export declare const toHaveArrayMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
