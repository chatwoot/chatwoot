declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is a `String` or `new String()`.
             * @param other
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ name: expect.toBeString() }));
             */
            toBeString<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is a `String` or `new String()`.
             * @example
             * expect(player.name).toBeString();
             */
            toBeString(): R;
        }
    }
}
export declare const toBeStringMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
