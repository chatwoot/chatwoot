declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is true.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveTrue('foo.bar')
             * );
             */
            toHaveTrue<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is true.
             * @example
             * expect({ foo: { bar: X } }).toHaveTrue('foo.bar');
             */
            toHaveTrue(propPath: string): R;
        }
    }
}
export declare const toHaveTrueMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
