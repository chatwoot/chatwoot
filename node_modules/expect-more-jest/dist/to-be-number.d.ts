declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is a valid `Number` or `new Number()` and not `NaN`.
             * @param other
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ age: expect.toBeNumber() }));
             */
            toBeNumber<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is a valid `Number` or `new Number()` and not `NaN`.
             * @example
             * expect(player.age).toBeNumber();
             */
            toBeNumber(): R;
        }
    }
}
export declare const toBeNumberMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
