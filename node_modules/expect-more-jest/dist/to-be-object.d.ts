declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is an `Object`.
             * @param other
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ player: expect.toBeObject() }));
             */
            toBeObject<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is an `Object`.
             * @example
             * expect(player).toBeObject();
             */
            toBeObject(): R;
        }
    }
}
export declare const toBeObjectMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
