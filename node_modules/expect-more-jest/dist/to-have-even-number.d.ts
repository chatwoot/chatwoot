declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is an even number or `new Number`.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveEvenNumber('foo.bar')
             * );
             */
            toHaveEvenNumber<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is an even number or `new Number`.
             * @example
             * expect({ foo: { bar: 4 } }).toHaveEvenNumber('foo.bar');
             */
            toHaveEvenNumber(propPath: string): R;
        }
    }
}
export declare const toHaveEvenNumberMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
