declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is a `Number` which is both greater than or equal to `floor` and less than or equal to
             * `ceiling`.
             * @param floor
             * @param ceiling
             * @example
             * expect(onPress).toHaveBeenCalledWith(
             *   expect.objectContaining({
             *     shotsRemaining: expect.toBeWithinRange(0, weapon.capacity)
             *   })
             * );
             */
            toBeWithinRange<T>(floor: number, ceiling: number): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is a `Number` which is both greater than or equal to `floor` and less than or equal to
             * `ceiling`.
             * @param floor
             * @param ceiling
             * @example
             * expect(weapon.shotsRemaining).toBeWithinRange(0, weapon.capacity);
             */
            toBeWithinRange(floor: number, ceiling: number): R;
        }
    }
}
export declare const toBeWithinRangeMatcher: (received: any, floor: number, ceiling: number) => {
    message: () => string;
    pass: boolean;
};
