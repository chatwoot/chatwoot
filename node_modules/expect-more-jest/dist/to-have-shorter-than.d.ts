declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is a string or array shorter than the given
             * string or array.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveShorterThan('foo.bar')
             * );
             */
            toHaveShorterThan<T>(propPath: string, other: string | any[]): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is a string or array shorter than the given
             * string or array.
             * @example
             * expect({ foo: { bar: X } }).toHaveShorterThan('foo.bar');
             */
            toHaveShorterThan(propPath: string, other: string | any[]): R;
        }
    }
}
export declare const toHaveShorterThanMatcher: (received: any, propPath: string, other: string | any[]) => {
    message: () => string;
    pass: boolean;
};
