declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that a value is a `Function`.
             * @param other
             * @example
             * expect(onPress).toHaveBeenCalledWith(expect.objectContaining({ shoot: expect.toBeFunction() }));
             */
            toBeFunction<T>(): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that a value is a `Function`.
             * @example
             * expect(player.shoot).toBeFunction();
             */
            toBeFunction(): R;
        }
    }
}
export declare const toBeFunctionMatcher: (received: any) => {
    message: () => string;
    pass: boolean;
};
