declare global {
    namespace jest {
        interface Expect {
            /**
             * Asserts that value has an own or nested named property which is an empty object.
             * @example
             * expect(received).toHaveBeenCalledWith(
             *   expect.toHaveEmptyObject('foo.bar')
             * );
             */
            toHaveEmptyObject<T>(propPath: string): Matchers<T>;
        }
        interface Matchers<R> {
            /**
             * Asserts that value has an own or nested named property which is an empty object.
             * @example
             * expect({ foo: { bar: X } }).toHaveEmptyObject('foo.bar');
             */
            toHaveEmptyObject(propPath: string): R;
        }
    }
}
export declare const toHaveEmptyObjectMatcher: (received: any, propPath: string) => {
    message: () => string;
    pass: boolean;
};
