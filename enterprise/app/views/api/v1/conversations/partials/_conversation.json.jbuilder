# Enterprise conversation partial
# Add any enterprise-specific conversation fields here
# For now, this is empty to prevent rendering errors

# Example enterprise fields (uncomment if needed):
# json.enterprise_field conversation.some_enterprise_attribute if conversation.respond_to?(:some_enterprise_attribute)
