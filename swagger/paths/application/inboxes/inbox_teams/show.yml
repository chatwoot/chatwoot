tags:
  - Inboxes
operationId: get-inbox-teams
summary: List Teams in Inbox
description: Get Details of Teams in an Inbox
security:
  - userApiKey: []
parameters:
  - $ref: '#/parameters/inbox_id'
responses:
  200:
    description: Success
    schema:
        type: array
        description: 'Array of all active teams'
        items:
          $ref: '#/definitions/team'
  404:
    description: Inbox not found
  403:
    description: Access denied
