tags:
  - Catalog Settings
operationId: create-catalog-settings
summary: Create Catalog Settings
description: Create catalog settings for the account.
security:
  - userApiKey: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/catalog_settings_create_update_payload'
responses:
  '201':
    description: Settings created
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/catalog_settings'
  '401':
    description: Unauthorized
  '422':
    description: Validation error
