tags:
  - Catalog Settings
operationId: update-catalog-settings
summary: Update Catalog Settings
description: Update the catalog settings for the account.
security:
  - userApiKey: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/catalog_settings_create_update_payload'
responses:
  '200':
    description: Settings updated
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/catalog_settings'
  '401':
    description: Unauthorized
  '422':
    description: Validation error
