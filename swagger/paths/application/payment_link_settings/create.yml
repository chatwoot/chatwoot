tags:
  - Payment Link Settings
operationId: create-payment-link-settings
summary: Create Payment Link Settings
description: Create payment link settings for the account.
security:
  - userApiKey: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/payment_link_settings_create_update_payload'
responses:
  '201':
    description: Settings created
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/payment_link_settings'
  '401':
    description: Unauthorized
  '422':
    description: Validation error
