tags:
  - Payment Link Settings
operationId: update-payment-link-settings
summary: Update Payment Link Settings
description: Update payment link settings for the account.
security:
  - userApiKey: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/payment_link_settings_create_update_payload'
responses:
  '200':
    description: Settings updated
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/payment_link_settings'
  '401':
    description: Unauthorized
  '422':
    description: Validation error
