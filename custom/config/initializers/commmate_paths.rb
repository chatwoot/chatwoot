# frozen_string_literal: true

# CommMate Custom Paths Configuration
# No longer needed - super admin views updated directly

Rails.application.config.to_prepare do
  Rails.logger.info '✅ CommMate paths configured' if Rails.logger
end

