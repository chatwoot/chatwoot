import { LazyLoadedDeadClicksAutocaptureInterface } from '../utils/globals';
import { PostHog } from '../posthog-core';
import { DeadClicksAutoCaptureConfig } from '../types';
declare class LazyLoadedDeadClicksAutocapture implements LazyLoadedDeadClicksAutocaptureInterface {
    readonly instance: PostHog;
    private _mutationObserver;
    private _lastMutation;
    private _lastSelectionChanged;
    private _clicks;
    private _checkClickTimer;
    private _config;
    private _onCapture;
    private _defaultConfig;
    private _asRequiredConfig;
    constructor(instance: PostHog, config?: DeadClicksAutoCaptureConfig);
    start(observerTarget: Node): void;
    private _startMutationObserver;
    stop(): void;
    private _onMutation;
    private _startClickObserver;
    private _onClick;
    private _startScrollObserver;
    private _onScroll;
    private _startSelectionChangedObserver;
    private _onSelectionChange;
    private _ignoreClick;
    private _checkClicks;
    private _captureDeadClick;
}
export default LazyLoadedDeadClicksAutocapture;
