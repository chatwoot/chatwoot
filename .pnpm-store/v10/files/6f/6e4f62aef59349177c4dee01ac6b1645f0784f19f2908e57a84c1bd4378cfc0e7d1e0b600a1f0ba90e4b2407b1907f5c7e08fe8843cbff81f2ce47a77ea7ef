const n = { name: "StubbedComponent", render: () => null };
function o(e) {
  for (const t of e)
    typeof t.type == "object" && t.type.name !== "HistoireVariant" && (t.type = n), Array.isArray(t.children) && o(t.children);
}
export {
  o as autoStubComponents
};
