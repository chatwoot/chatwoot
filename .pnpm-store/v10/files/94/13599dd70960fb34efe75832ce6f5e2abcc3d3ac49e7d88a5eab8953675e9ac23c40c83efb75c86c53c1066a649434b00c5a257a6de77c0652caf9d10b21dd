
{{alias}}()
    Tests for native `Float64Array` support.

    Returns
    -------
    bool: boolean
        Boolean indicating if an environment has `Float64Array` support.

    Examples
    --------
    > var bool = {{alias}}()
    <boolean>

    See Also
    --------

